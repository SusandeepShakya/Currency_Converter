import moment from "moment/moment";

export const url_params = {
    page: 1,
    per_page: '',
    from: '', /* moment().subtract(3, 'months').format('Y-M-d') */
    to: '', /* moment().format('Y-M-d') */
}